# System Tests

Belongs here: future end-to-end tests that validate complete WorkflowForge process behavior across API, worker, scheduler, and web surfaces.

Does not belong here: unit tests, architecture boundary checks, or provider-specific adapter tests.

Owner: system reliability.

Dependency direction: should interact with running processes through public interfaces.
